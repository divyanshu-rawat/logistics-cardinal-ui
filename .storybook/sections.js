const separator = '->';

export const COMPONENTS = `Components${separator}`;
export const FORM = `Form${separator}`;
export const GRID = `Grid${separator}`;
export const TYPOGRAPHY = `Typography${separator}`;
export const ICONS = `Icons${separator}`;
export const COMPOSITION = `Composition${separator}`;
