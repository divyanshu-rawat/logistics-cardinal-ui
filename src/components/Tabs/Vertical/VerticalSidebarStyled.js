import styled from 'styled-components';

const VerticalSidebarStyled = styled('div')`
  background-color: ${({ theme }) => theme.verticalTabSidebarBackgroundColor};
  min-width: 220px;
`;

export default VerticalSidebarStyled;
